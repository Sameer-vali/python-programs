a = [2,5,-5,14,8,9,-11]
b = 2

max_sum = float('-inf')

for i in range(0, len(a), b):
    if i + b <= len(a):

        current_sum = 0

        for j in range(i, i + b):
            current_sum += a[j]

        max_sum = max(max_sum, current_sum)

print(max_sum)
